# 基础信息（通用配置）
#------------------------------------------------

PUBLIC_GITHUB_PROFILE='https://github.com/<YOUR_USERNAME>'  # 你的 GitHub 主页
PUBLIC_REPOSITORY_URL='https://github.com/<YOUR_USERNAME>/<YOUR_REPO>'  # 仓库主页地址
PUBLIC_DOCS_URL='https://github.com/<YOUR_USERNAME>/<YOUR_REPO>/blob/main/README.md'  # 文档/说明书链接
PUBLIC_GITHUB_STARS_URL='https://api.github.com/repos/<YOUR_USERNAME>/<YOUR_REPO>'  # 获取仓库 Star 数量的 GitHub API 接口


# 应用相关配置（前端 / 接口）
#------------------------------------------------

PUBLIC_BASE_URL='http://localhost:5173'  # 前端网站根域名（部署到生产后改成正式域名）
PUBLIC_OPENAI_MODEL='gpt-3.5-turbo'  # 使用的 OpenAI 模型（可以改成更高版本）
OPENAI_API_KEY='YOUR_API_KEY'  # OpenAI 接口密钥，用于调用 AI 模型
FORTUNE_API='YOUR_FORTUNE_API'  # 自定义“今日签文”或“随机语录”API
BITCOIN_PREDICTION_API='YOUR_PREDICTION_API'  # 比特币价格预测接口
N8N_AGENT_API='YOUR_N8N_ENDPOINT'  # n8n 自动化工作流接口（可用于自定义任务触发）


# 数据库配置
#------------------------------------------------

PUBLIC_POCKETBASE_URL='https://api.maigewan.com/api/'  # PocketBase 主接口地址（如 http://127.0.0.1:8090）
PUBLIC_POCKETBASE_ADMIN='https://api.maigewan.com/_/'  # PocketBase 后台管理端地址（如 http://127.0.0.1:8090/_）
REDIRECT_URL='https://api.maigewan.com/api/oauth2-redirect'  # OAuth 登录授权回调地址


# 表单配置（Google 表单 API）
#------------------------------------------------

GOOGLE_FORM_URL='https://docs.google.com/forms/d/e/<YOUR_GOOGLE_FORM_ID>/formResponse'  # Google 表单提交接口

FORM_PAYLOAD_FIRST_NAME='entry.YOUR_PAYLOAD_ID'  # 表单字段：名
FORM_PAYLOAD_LAST_NAME='entry.YOUR_PAYLOAD_ID'  # 表单字段：姓
FORM_PAYLOAD_TYPE='entry.YOUR_PAYLOAD_ID'  # 表单字段：类型（例如问题类型或反馈类别）
FORM_PAYLOAD_PRIORITY='entry.YOUR_PAYLOAD_ID'  # 表单字段：优先级
FORM_PAYLOAD_MESSAGE='entry.YOUR_PAYLOAD_ID'  # 表单字段：留言内容
FORM_PAYLOAD_EMAIL='emailAddress'  # 表单字段：电子邮箱


# Stripe 支付配置
#------------------------------------------------

SECRET_STRIPE_KEY='YOUR_SECRET_KEY'  # Stripe 后端密钥，用于创建支付意图
PUBLIC_STRIPE_KEY='YOUR_PUBLIC_KEY'  # Stripe 前端公开密钥，用于加载支付界面


# 比特币捐赠配置
#------------------------------------------------

PUBLIC_BTC_DONATION_ADDRESS='YOUR_BTC_ADDRESS'  # 公共比特币收款地址（用于接受捐赠）
